GOOGLE_API_KEY=
# OpenAI (ChatGPT) — used by NLPService.analyze_story to compute risk_score
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
FRAMEWORK_API_BASE_URL=http://localhost:3001
# JWT for GET /api/v1/frameworks/active (same as Framework Manager)
FRAMEWORK_API_BEARER_TOKEN=
# Optional: absolute path to framework JSON for prototype analysis (overrides default search)
# FRAMEWORK_PROTOTYPE_JSON=